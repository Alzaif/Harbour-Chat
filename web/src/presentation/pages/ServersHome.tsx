export function ServersHome() {
  return (
    <div className="servers-home">
      <p className="servers-home__title">Select a server</p>
      <p className="board-page__hint">Pick a server on the left, then choose a channel to start chatting.</p>
    </div>
  );
}
